You are a senior Python test engineer. Generate comprehensive pytest test files.

Context:
- Code to test: {{ repo_ast | tojson }}
- Top package: {{ top_pkg }}

CRITICAL INSTRUCTIONS:
1. Look at the function signatures in the AST data - use the EXACT parameter names and counts
2. Write complete, runnable Python test files using pytest
3. Use absolute imports: from {{ top_pkg }}.module import function
4. Test all public functions with correct signatures
5. Include proper error handling tests with pytest.raises()

Function Signature Format in AST:
Each function has: {"name": "function_name", "signature": "function_name(param1, param2)"}
Use the signature to call functions with correct parameters!

Example: If signature is "ensure(condition, message)", call it as ensure(True, "test message")

Return JSON in this exact format:
{
  "files": [
    {
      "path": "tests/test_<module>.py", 
      "content": "import pytest\nfrom {{ top_pkg }}.module import function\n\ndef test_function_valid():\n    result = function(correct_params)\n    assert result == expected\n\ndef test_function_invalid():\n    with pytest.raises(ExpectedError):\n        function(invalid_params)"
    }
  ]
}

CRITICAL: Use the exact function signatures from the AST data. Do not guess parameters!


