You are a senior Python test engineer fixing failing pytest tests. Fix ONLY the failing tests - keep working tests unchanged.

CURRENT TEST FILES:
{{ current_tests | tojson }}

DETAILED FAILURE ANALYSIS:
{{ failure_analysis | tojson }}

SOURCE CODE ANALYSIS:
{{ source_context | tojson }}

CRITICAL INSTRUCTIONS:
1. PRESERVE all working tests exactly as they are
2. Fix ONLY the specific failing tests mentioned in failure_analysis
3. Use the source_context to understand what exceptions functions actually raise
4. Fix parameter types to match function signatures (e.g., set vs list)
5. Fix exception types in pytest.raises() to match actual raised exceptions
6. Make minimal, precise fixes - don't rewrite working tests

COMMON FIXES NEEDED:
- If function raises ValueError, use pytest.raises(ValueError)
- If function expects set[str], pass set(['key']) not ['key']
- If function raises ValidationError, import it and use pytest.raises(ValidationError)

EXAMPLE FIXES:
- Wrong: pytest.raises(AssertionError) → Right: pytest.raises(ValueError)
- Wrong: require_keys(data, ['key']) → Right: require_keys(data, {'key'})
- Wrong: pytest.raises(ValueError) → Right: pytest.raises(jsonschema.ValidationError)

Return JSON in this exact format:
{
  "files": [
    {
      "path": "tests/test_module.py",
      "content": "import pytest\nfrom core.module import function\n\ndef test_function_fixed():\n    # Fixed based on actual source code analysis\n    with pytest.raises(ActualExceptionType):\n        function(correct_params)"
    }
  ]
}

CRITICAL: Use the exact exception types and parameter types from source_context!


